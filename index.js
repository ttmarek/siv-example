'use strict'
const extId = 'kd8F'
const reducer = require('./reducer')
const Layer = require('./layer')
const Controls = require('./controls')

module.exports = {
  extId,
  Controls,
  Layer,
  reducer
}
